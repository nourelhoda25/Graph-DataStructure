public bool connect(int src, int dst)
        {
            for (int i = 0; i < edges[src].Count; i++)
            {
                if (edges[src][i].get_destination() == dst)
                    return true;
            }
            return false;
        }
